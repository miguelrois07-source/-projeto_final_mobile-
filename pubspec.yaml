name: projeto_final
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.3.2
  

flutter:
  uses-material-design: true

  assets:
    - assets/images/reaproveita.svg